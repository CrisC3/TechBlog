<div class="card">
    <div class="card-header">
       <h1>{{title}}</h1>
        <p>Posted by {{'User.username'}} on {{format_date createdAt}}</p>
    </div>
    <p class="card-body">
        {{content}}
    </p>
</div>